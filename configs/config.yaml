baseline:
  name: "baseline"
  data:
    out_format: "png"
    train:
      limit: false
      patchify: true
      augment_before:
      augment_after:
      patch:
        size_x: 128
        size_y: 128
        step: 128
    test:
      limit: false
      patchify: true
      augment_before:
      augment_after:
      patch:
        size_x: 128
        size_y: 128
        step: 128

  unet:
    depth: 4
    pool_factor: 2

  train:
    max_epochs: 50
    learning_rate: 0.0002
    batch_size: 32
    use_validation: true
    validation_size: 0.15  # of trainings data

  test:
    use_best_model: true  # if false load from model_name
    model_name: "best_model.ckpt"

#baseline_large_patch:
# always OOM patch size too large
#  name: "baseline_large_patch"
#  data:
#    out_format: "png"
#    train:
#      limit: false
#      patchify: true
#      augment_before:
#      augment_after:
#      patch:
#        size_x: 256
#        size_y: 256
#        step: 256
#    test:
#      limit: false
#      patchify: true
#      augment_before:
#      augment_after:
#      patch:
#        size_x: 256
#        size_y: 256
#        step: 256
#
#  unet:
#    depth: 4
#    pool_factor: 4
#
#  train:
#    max_epochs: 200
#    learning_rate: 0.0005
#    batch_size: 2
#    use_validation: true
#    validation_size: 0.15  # of trainings data
#  test:
#    use_best_model: true  # if false load from model_name
#    model_name: "best_model.ckpt"

baseline_small_patch:
  name: "baseline_small_patch"
  data:
    out_format: "png"
    train:
      limit: false
      patchify: true
      augment_before:
      augment_after:
      patch:
        size_x: 32
        size_y: 32
        step: 32
    test:
      limit: false
      patchify: true
      augment_before:
      augment_after:
      patch:
        size_x: 32
        size_y: 32
        step: 32

  unet:
    depth: 4
    pool_factor: 2

  train:
    max_epochs: 200
    learning_rate: 0.0005
    batch_size: 64
    use_validation: true
    validation_size: 0.15  # of trainings data
  test:
    use_best_model: true  # if false load from model_name
    model_name: "best_model.ckpt"

baseline_small_patch_deep:
  name: "baseline_small_patch_deep"
  data:
    out_format: "png"
    train:
      limit: false
      patchify: true
      augment_before:
      augment_after:
      patch:
        size_x: 32
        size_y: 32
        step: 32
    test:
      limit: false
      patchify: true
      augment_before:
      augment_after:
      patch:
        size_x: 32
        size_y: 32
        step: 32

  unet:
    depth: 8
    pool_factor: 2

  train:
    max_epochs: 200
    learning_rate: 0.0005
    batch_size: 64
    use_validation: true
    validation_size: 0.15  # of trainings data
  test:
    use_best_model: true  # if false load from model_name
    model_name: "best_model.ckpt"
